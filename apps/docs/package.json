{
  "name": "docs",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "description": "PixelKit documentation site (Astro Starlight). Renders the markdown in the repo-root docs/ directory; see scripts/sync-docs.mjs.",
  "scripts": {
    "sync": "node scripts/sync-docs.mjs",
    "dev": "npm run sync && astro dev",
    "start": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.42.0",
    "astro": "^7.2.10",
    "sharp": "^0.35.3"
  }
}
